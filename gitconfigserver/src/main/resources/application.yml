server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wanggja/springcloud
          username: #
          password: #
          search-paths: config
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/

