spring:
  cloud:
    config:
      name: configclient
      label: master
      discovery:
        enabled: true
        serviceId: gitconfigserver

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/