spring:
  cloud:
    config:
      name: configclient
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: gitconfigserver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/