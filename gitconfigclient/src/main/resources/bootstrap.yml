spring:
  cloud:
    config:
      uri: http://locahost:8888
      label: master
      profile: dev
  application:
    name: config-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka/