spring:
  cloud:
    config:
      name: config
      label: master
      discovery:
        enabled: true
        service-id: gitconfigserver

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/